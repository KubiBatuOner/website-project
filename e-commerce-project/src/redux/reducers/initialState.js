const initialState = {
    // user authentication
    isAuthenticated: false,

    // user
    user: null,

    // loading
    loading: false,

    // error
    error: null,
};

export default initialState;